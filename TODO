Features :
    -> Mise en cache LRU/LFU TCPHandler
    -> DIGEST_LENGTH => double de la taille necessaire
    -> comparer LFU/LRU/ARC : BTree
    -> ARC au lieu de LRU : Btree??
    -> Use CRUSH as placement algorithme to have optimal reorganization ?
    -> load partiel 
    -> load_mem on retourne un m=pointeur en memoire( mmap )
    -> Système de cache coté client ??
    -> Fichier de conf et de log
    -> Biding python ?
    -> SSE instruction
    -> code correction metadata
    
    -> Optimisation power8( in my dream )
